package org.foden.chanllenges;

public class CreateImmutableClass {
    /**
     * An immutable class is one whose objects cannot be modified after creation.
     * Declare the class as final (cannot be subclasses).
     * Make all fields private and final.
     * Don't provide setters.
     * Initialize fields via constructor.
     * Return deep copies of mutable objects in getters.
     * */
}
